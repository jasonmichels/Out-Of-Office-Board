---
# PHP Tools
- name: Composer
  shell: curl -sS https://getcomposer.org/installer | php && mv composer.phar /usr/local/bin/composer creates=/usr/local/bin/composer

- name: Run composer install on project
  composer: working_dir={{ project_path }}